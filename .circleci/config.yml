version: 2.1
jobs:
  build:
    docker:
      - image: circleci/python:3.9
    steps:
      - checkout
      - run:
          name: Build and Test
          command: |
            python main.py

workflows:
  version: 2
  build-deploy:
    jobs:
      - build
