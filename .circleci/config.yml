version: 2.1
jobs:
  build:
    working_directory: ~/circleci-python
    docker:
      - image: circleci/python:3.9
    steps:
      - checkout
      - run: python3 main.py 5 10  # Replace 5 and 10 with your desired numbers

workflows:
  build:
    jobs:
      - build
